package com.uludag.can.jibbledevelopertest.listeners

// This listener is for the communication between activity and adapter when an item clicked
interface DisplayDataDetailListener {
    fun displayDataDetail(dataPosition: Int)
}